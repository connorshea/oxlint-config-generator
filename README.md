# Oxlint Config Generator

A simple Vue application to generate Oxlint configuration files interactively.

To set up the project locally and perform dev tasks, use the following commands:

```sh
pnpm install # Install dependencies
pnpm lint # Lint the codebase using Oxlint
pnpm fmt  # Format the codebase using Oxfmt
```
